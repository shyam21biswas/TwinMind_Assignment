package com.example.shyam_assignment.ui.theme

import androidx.compose.ui.graphics.Color

// ── TwinMind Light Theme Colors ────────────────────────────────────────
// Inspired by the TwinMind app screenshot — warm, calm, professional.

val TwinBackground = Color(0xFFF7F7F3)        // Warm off-white background
val TwinSurface = Color(0xFFFFFFFF)            // Pure white for cards
val TwinElevatedCard = Color(0xFFEAF2F2)       // Light teal-tinted card fill
val TwinPrimary = Color(0xFF0D7377)            // Deep teal — main accent color
val TwinSecondary = Color(0xFFE07C3A)          // Warm orange — secondary accent
val TwinTextPrimary = Color(0xFF1B3A4B)        // Dark navy — headings and body text
val TwinTextSecondary = Color(0xFF6B8299)      // Muted blue-gray — secondary text
val TwinWarning = Color(0xFFE07C3A)            // Orange — warning messages
val TwinError = Color(0xFFD94444)              // Red — error states

val TwinOnPrimary = Color(0xFFFFFFFF)          // White text on teal buttons
val TwinOutline = Color(0xFFD0D8DD)            // Light gray — card borders
val TwinSurfaceVariant = Color(0xFFF0F2EE)     // Subtle warm gray — alternate surfaces

val TwinAccentGreen = Color(0xFF2E8B57)        // Green — success indicators
val TwinGlow = Color(0xFF0D7377)               // Teal glow for recording button
val TwinCardBorder = Color(0xFFD0D8DD)         // Light border for cards
val TwinRecordingRed = Color(0xFFD94444)       // Red — recording indicator and stop button
val TwinGradientStart = Color(0xFF0D7377)      // Teal gradient start (accent bars)
val TwinGradientEnd = Color(0xFF149DA0)        // Lighter teal gradient end
